//Guarda la URL del back, si cargamos un deploy, se cambia en true
//y la URL a una real.
//Harcodear : escribir codigo fijo fuente, busca este dato en la maquina del user y este necesita cambiar la url en donde esta siendo llamada. 



export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000/api'
};